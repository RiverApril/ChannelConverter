cmake_minimum_required(VERSION 2.8)
project(ChannelConverter)
find_package(OpenCV REQUIRED)
add_executable(ChannelConverter ChannelConverter.cpp)
target_link_libraries(ChannelConverter ${OpenCV_LIBS})